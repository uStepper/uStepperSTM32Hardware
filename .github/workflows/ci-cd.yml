name: CI/CD Pipeline

on:
  push:
    branches: [ feature-CI/CD ]

permissions:
  contents: write

jobs:
  build-zip-bundle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Compress
        run: |
          cd ..
          echo "Workspace:"
          ls -la
          echo "Repo root:"
          ls -la uStepperSTM32Hardware
          tar -cjf uStepperSTM32Hardware.tar.bz2 -C ..\
            uStepperSTM32Hardware/cores \
            uStepperSTM32Hardware/libraries \
            uStepperSTM32Hardware/system \
            uStepperSTM32Hardware/variants \
            uStepperSTM32Hardware/tools \
            uStepperSTM32Hardware/boards.txt \
            uStepperSTM32Hardware/kerywords.txt \
            uStepperSTM32Hardware/platform.txt \
            uStepperSTM32Hardware/programmers.txt
      - name: Upload tar artifact
        uses: actions/upload-artifact@v4
        with:
          name: uStepperSTM32Hardware-tar-bundle
          path: uStepperSTM32Hardware.tar.bz2
          
  build:
    needs: [ build-zip-bundle]
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Download archive artifact
      uses: actions/download-artifact@v4
      with:
        name: uStepperSTM32Hardware-tar-bundle
        path: .

    - name: List tar contents
      run: |
        tar -tjf uStepperSTM32Hardware.tar.bz2
